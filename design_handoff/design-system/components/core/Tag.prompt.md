**Tag** — metadata chip for game / format / reward info.

```jsx
<Tag>5v5 · LoL</Tag>
<Tag tone="accent">+250 EXP</Tag>
<Tag tone="cyan" icon={<Icon name="trophy" />}>Prize pool</Tag>
```
Use for non-status metadata (game, mode, region, reward). For state (Open/Live/Ended) use Badge instead.
