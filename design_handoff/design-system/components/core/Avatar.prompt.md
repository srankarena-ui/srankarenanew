**Avatar** — player image or initials on a blue→cyan gradient.

```jsx
<Avatar name="Nova" size="lg" />
<Avatar name="Kx" rank="S" />          {/* rank-tinted ring */}
<Avatar src={url} name="Player" shape="circle" />
```
Sizes xs→xl. `rank` (F–S) draws a colored ring. Default shape is a friendly squircle; `circle` available.
