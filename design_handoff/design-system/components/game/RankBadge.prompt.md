**RankBadge** — the player's competitive rank (F → S), tinted to its signature color.

```jsx
<RankBadge rank="S" size="lg" glow />
<RankBadge rank="B" />
```
Sizes sm/md/lg. Use `glow` for the player's own current rank (hero contexts). Colors come from the `--rank-*` tokens.
